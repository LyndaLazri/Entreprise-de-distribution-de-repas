package Project;

public class Point {
		// déclaration des attributs de this (un point de la famille)
		double x; // abcisse
		double y; // ordonnée
		
		Point(double a, double b) {
			// effet : initialiser les attributs de this
			this.x = a;
			this.y = b;
			}
}
